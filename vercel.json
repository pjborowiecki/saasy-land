{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "installCommand": "bun install",
  "buildCommand": "bun run db:migrate && (bun --bun next build || (echo 'first attempt crashed; clean retry' && rm -rf .next && bun --bun next build))",
  "regions": ["fra1"]
}
